<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.ritterweb.checkitv01">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Checkit">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- die nav-graph zeile wird eingefügt um mit dem nav-graph deeplinks machen zu können siehe Stelle 5:27 in https://www.youtube.com/watch?v=hEJ78_KLcfA&list=PLrnPJCHvNZuCamMFswP597mUF-whXoAA6&index=9   -->
            <nav-graph android:value="@navigation/nav_grph" />
        </activity>
    </application>

</manifest>